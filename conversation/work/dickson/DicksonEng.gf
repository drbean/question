concrete DicksonEng of Dickson = DicksonI with

	(Syntax = SyntaxEng),
	(LexDickson = LexDicksonEng)  **
		open ResEng in {

	lin who_IP =
		{s = table NPCase ["who"; "whose"; "whom"; "whose"];
		 n = Sg; lock_IP = <>};
}
